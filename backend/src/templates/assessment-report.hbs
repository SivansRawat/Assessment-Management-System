<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{assessmentName}} Report</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #fff;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 3px solid #007bff;
        }

        .header h1 {
            color: #007bff;
            font-size: 28px;
            margin-bottom: 10px;
            font-weight: bold;
        }

        .header .meta {
            color: #666;
            font-size: 14px;
        }

        .section {
            margin-bottom: 30px;
            background: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
            border-left: 4px solid #007bff;
        }

        .section h2 {
            color: #007bff;
            font-size: 20px;
            margin-bottom: 15px;
            padding-bottom: 8px;
            border-bottom: 1px solid #dee2e6;
        }

        .fields-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 15px;
        }

        .field {
            background: white;
            padding: 15px;
            border-radius: 6px;
            border: 1px solid #e9ecef;
            position: relative;
        }

        .field-label {
            font-weight: bold;
            color: #495057;
            font-size: 14px;
            margin-bottom: 5px;
        }

        .field-value {
            font-size: 18px;
            font-weight: 600;
            margin-bottom: 5px;
        }

        .field-classification {
            display: inline-block;
            padding: 2px 8px;
            border-radius: 12px;
            font-size: 12px;
            font-weight: bold;
            color: white;
            margin-left: 8px;
        }

        .classification-green {
            background-color: #28a745;
        }

        .classification-yellow {
            background-color: #ffc107;
            color: #212529 !important;
        }

        .classification-red {
            background-color: #dc3545;
        }

        .classification-blue {
            background-color: #007bff;
        }

        .footer {
            margin-top: 40px;
            text-align: center;
            padding-top: 20px;
            border-top: 1px solid #dee2e6;
            color: #6c757d;
            font-size: 12px;
        }

        @media print {
            .container {
                max-width: 100%;
                padding: 10px;
            }

            .section {
                break-inside: avoid;
                page-break-inside: avoid;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>{{assessmentName}}</h1>
            <div class="meta">
                <p><strong>Session ID:</strong> {{sessionId}}</p>
                <p><strong>Generated:</strong> {{formatDate generatedAt}}</p>
            </div>
        </div>

        {{#if sections}}
        {{#each sections}}
        <div class="section">
            <h2>{{title}}</h2>
            <div class="fields-grid">
                {{#each fields}}
                <div class="field">
                    <div class="field-label">{{label}}</div>
                    <div class="field-value">
                        {{formatValue value format unit}}
                        {{#if classification}}
                            {{#with (getClassification value classification)}}
                                {{#if label}}
                                <span class="field-classification classification-{{color}}">{{label}}</span>
                                {{/if}}
                            {{/with}}
                        {{/if}}
                    </div>
                </div>
                {{/each}}
            </div>
        </div>
        {{/each}}
        {{else}}
        <div class="section">
            <h2>No Data Available</h2>
            <p>No assessment data could be processed for this report.</p>
        </div>
        {{/if}}

        <div class="footer">
            <p>This report was automatically generated by the Assessment Management System</p>
            <p>Generated on {{formatDate generatedAt}} | Session: {{sessionId}}</p>
        </div>
    </div>
</body>
</html>
